# GitOpenClaw — Not Yet Installed

This file is an installation status flag.

When the `GITOPENCLAW-INSTALLER.yml` workflow completes successfully, it
renames this file to `GITOPENCLAW-INSTALLED.md` as a visible record that
the bootstrap process has run.

If you see this file, the installer has **not** been run yet.
